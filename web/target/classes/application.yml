spring:
  profiles:
    active: dev
  jackson: #设置时间格式和时区
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/static/